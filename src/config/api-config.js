const API_CONFIG = {
  // If your backend is running on a different port, use the full URL
  // baseURL: 'http://localhost:5300/api'
  
  // If using Vite's proxy or if frontend/backend are on same origin
  baseURL: '/api'
};

export default API_CONFIG;